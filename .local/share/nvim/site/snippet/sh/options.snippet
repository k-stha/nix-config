while [ "$#" -gt 0 ]; do
    case "${1}" in
        -h|--help)
            display_help
            exit 0
            ;;
        -*)
            die "Invalid option: ${1}"
            ;;
        *)
            die "Invalid option: ${1}"
            ;;
    esac
done
